name: Deploy

on:
  workflow_call:
    secrets:
      rockeset-token:
        required: true

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: prueba
        run: echo ${{ secrets.rockeset-token }}
